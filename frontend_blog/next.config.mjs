/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['m.media-amazon.com','res.cloudinary.com', 'f.media-amazon.com']
  },
};

export default nextConfig;
